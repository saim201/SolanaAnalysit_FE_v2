[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # VITE_API_URL will be set in Netlify dashboard Environment Variables

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

